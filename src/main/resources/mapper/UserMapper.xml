<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bikalp.roomrentalservice.mapper.UserMapper">
    <select id="getUserById" resultType="com.bikalp.roomrentalservice.dto.response.UserResponse">
        select id           as userId,
               full_name    as fullName,
               email        as email,
               username     as username,
               phone_number as phoneNumber,
               user_role    as role,
               is_active    as isActive
        from users
        where id = #{userId} and is_active
    </select>
    <select id="getAllUsers" resultType="com.bikalp.roomrentalservice.dto.response.UserResponse">
        select id           as userId,
               full_name    as fullName,
               email        as email,
               username     as username,
               phone_number as phoneNumber,
               user_role    as role,
               is_active    as isActive
        from users where is_active order by updated_at desc
    </select>
    <select id="getLoggedInUserInfoByUserId" resultType="com.bikalp.roomrentalservice.dto.response.LoggedInUserInfo">
        select id           as userId,
               full_name    as fullName,
               email        as email,
               username     as username,
               phone_number as phoneNumber,
               user_role    as role,
               is_active    as isActive
        from users
        where id = #{userId} and is_active
    </select>
    <select id="getPendingApprovalsForRegistrations" resultType="com.bikalp.roomrentalservice.dto.response.UserResponse">
        select id             as userId,
               full_name      as fullName,
               email          as email,
               username       as username,
               phone_number   as phoneNumber,
               user_role      as role,
               is_active      as isActive,
               account_status as accountStatus
        from users
        where is_active and account_status='PENDING'
    </select>
</mapper>